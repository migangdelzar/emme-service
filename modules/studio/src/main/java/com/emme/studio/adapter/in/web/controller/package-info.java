/**
 * HTTP controllers for the core Studio capability.
 *
 * <p>Controllers translate versioned HTTP requests into public use-case calls. They must not access
 * persistence repositories, domain entities from another module, or provider clients.
 */
package com.emme.studio.adapter.in.web.controller;
