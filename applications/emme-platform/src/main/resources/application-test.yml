spring:
  application:
    name: emme-platform
  datasource:
    url: jdbc:h2:mem:emme;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: false
  modulith:
    events:
      externalization:
        enabled: false
      jdbc:
        schema-initialization:
          enabled: true

logging:
  level:
    com.emme: DEBUG
