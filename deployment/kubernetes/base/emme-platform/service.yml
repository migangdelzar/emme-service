apiVersion: v1
kind: Service
metadata:
  name: emme-platform
  labels:
    app: emme-platform
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: emme-platform
