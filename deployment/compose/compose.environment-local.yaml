# Local environment overlay. Select a runtime image in
# compose.runtime-jvm.yaml or compose.runtime-native.yaml before applying this
# file.
services:
  emme-platform:
    environment:
      SPRING_PROFILES_ACTIVE: local
      SPRING_DEVTOOLS_RESTART_ENABLED: "true"
    ports:
      - "8081:8081"
