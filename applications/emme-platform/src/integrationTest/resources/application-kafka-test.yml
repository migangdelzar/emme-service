spring:
  application:
    name: emme-platform-kafka-test
  task:
    scheduling:
      enabled: false

app:
  tenant:
    provisioning:
      enabled: false
  datasource:
    url: jdbc:h2:mem:emme-kafka;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
  liquibase:
    enabled: false
  modulith:
    events:
      jdbc:
        schema-initialization:
          enabled: true

logging:
  level:
    org.springframework.kafka: WARN
    org.springframework.modulith: INFO
