/**
 * Inbound webhook adapters for external payment providers.
 *
 * <p>This package owns transport parsing, signature verification, replay protection, and delegation
 * to payment use cases. It does not own payment lifecycle rules.
 */
package com.emme.payment.adapter.in.webhook;
