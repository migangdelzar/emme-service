# Local dev — mounts source for hot reload
services:
  emme-platform:
    build:
      context: ../..
    environment:
      SPRING_PROFILES_ACTIVE: local
      SPRING_DEVTOOLS_RESTART_ENABLED: "true"
    ports:
      - "8081:8081"
