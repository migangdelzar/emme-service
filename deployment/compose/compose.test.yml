# CI test — depends on healthy dependencies
services:
  studio-api:
    environment:
      SPRING_PROFILES_ACTIVE: test
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
